# Plan: Tier 3 Backlog

slug: tier-3-roadmap
created: 2026-06-18T22:50:00Z
status: draft

## Findings

Two streams of work consolidated here:

1. End-to-end audit of Riff (UX polish, bugs, parity gaps).
2. Competitor UX research across Spotify, Apple Music, YT Music + Amazon Music, and niche/emerging players (Tidal, SoundCloud, Marvis, Doppler, Roon, last.fm, Pandora, Bandcamp, Endel).

Cross-cutting takeaways:

- The single biggest near-term win is restoring the audio-to-video toggle. Riff currently hides YT Music's defining feature.
- BYO-LLM is under-leveraged. The Anthropic key is wired to Queue Builder only; X-Ray cards, lyric translation, Smart Sections, AI DJ commentary, magazine artist bios, and "ask about your year" all sit on the same hook.
- macOS-native chrome (Mini Player window, menu bar, Notification Center widget, expanded Shortcuts) is the moat against the YT Music webapp.
- Three P0 bugs and five 15-min UX wins are blocking polish. Fix that batch before adding anything new.

The CLAUDE.md architectural rule still holds: WKWebView is audio-only and invisible except for sign-in. The audio-to-video toggle is the one scoped exception, surfaced as a discrete dismissible video pane on Now Playing.

## Steps

### Tier A: Ship next 2 weeks (P0 fixes + highest-leverage features)

- [x] **A1. P0 bug fixes** — force-unwraps on URL construction (PlayerBridge.swift:725, InnerTubeClient.swift:373-375), add 10s timeout to InnerTubeClient URLSession, max-attempts cap on OAuth Device Flow polling (OAuthDeviceFlow.swift:147).
  - note: Replaced both URL force-unwraps with safe `guard` fallbacks (home URL for player, `InnerTubeError.decoding` for Data API). Swapped `URLSession.shared` for a configured `URLSession` with 10s request / 30s resource timeouts; tests still inject custom sessions via `URLSession?` default. Added 400-attempt cap to OAuth polling loop with distinct timeout vs. max-attempts failure messages.
- [x] **A2. UX quick-wins batch** — help tooltips on icon-only buttons, bump secondary text contrast to opacity 0.75, restore menu-indicator automatic on dropdown buttons, ellipsis plus tooltip on tile subtitles, empty-state when search field cleared.
  - note: Threaded a `help:` parameter through `controlButton` in MiniPlayer/NowPlaying and added tooltips on play/pause, prev/next, skip, like, shuffle, repeat, more, add-to-playlist, sleep timer, playback rate, and the Library sort menu. Swept secondary `foregroundStyle(.white.opacity(0.5/0.55/0.6))` → `0.75` across 15 UI files. Removed `.menuIndicator(.hidden)` on the three label-style dropdowns (Library sort, sleep timer, playback rate) while keeping it hidden on icon-only overflow menus (•••, add-to-playlist plus). Added `.help(title — subtitle)` and explicit tail truncation on `ThumbnailButton` tiles. Added `InitialSearchState` "Find your music" empty state so the Search tab no longer renders blank before the user types.
- [x] **A3. Sleep timer: fade-out + end-of-track modes** — extend existing timer with gentle 10s volume ramp to silence, and "stop after current track ends". Spotify still does not ship this on desktop.
  - note: Added `PlayerBridge.SleepTimerMode` (`.hardStop` / `.fadeOut` / `.endOfTrack`) and a `setSleepTimer(minutes:mode:)` overload defaulting to `.hardStop` for back-compat. `.fadeOut` runs a 10-step linear ramp on `<video>.volume` (multiplying the user's current volume by an interpolated factor so the fade scales with whatever level they picked), then pauses and re-pushes the persisted volume to the JS bridge. `.endOfTrack` flips a new `endOfTrackArmed` flag; the existing `.ended` event handler honors it by pausing instead of advancing to user-queued items. `cancelSleepTimer` restores the JS-side volume if cancel lands mid-fade. NowPlaying sleep menu refactored into per-duration `Stop` / `Fade out (10s)` submenus plus a top-level "End of current track" entry; tooltip + small `EOT` chip surface the active mode without reopening the menu. Added `SleepTimerModeTests` to guard the enum + 10s constant against silent renames.
- [x] **A4. Audio-to-Video toggle on Now Playing** — pill at top of Now Playing that swaps between audio-only and the official music video, preserving currentTime. YT Music's defining feature; currently hidden.
  - note: Added `PlayerBridge.isVideoVisible` (defaults false) plus a `hostedWebView` accessor and `reattachWebViewOffscreen()` helper that delegates to a new `HiddenPlayerWebView.reattachToOffscreenWindow()`. NowPlaying's top bar gains a Song / Video segmented pill (Theme.red highlight on the active side); flipping to Video swaps the 320×320 artwork for a new `VideoPaneView` NSViewRepresentable that reparents the existing offscreen WKWebView into a 480×270 (16:9) rounded pane. Same `<video>` keeps playing so currentTime / audio are preserved across the toggle with no seek. Coordinator + `dismantleNSView` reattach the WebView to its 1×1 offscreen window when the pane is removed; `.onDisappear` + the Close / ESC paths also force `isVideoVisible = false` so the architectural rule (WebView never visible outside this opt-in pane) holds on next open. Strict Swift 6: VideoPaneView's makeNSView / updateNSView / Coordinator are explicitly `@MainActor`; `dismantleNSView` uses `MainActor.assumeIsolated`.
- [x] **A5. "Other performances" row** — surface live, acoustic, and cover versions from /next response. We already fetch this data and throw it away.
  - note: Added `InnerTubeClient.relatedSections(browseId:)` + a static `parseRelatedSections(_:)` helper that walks the Related-tab browse response (single-column, two-column desktop, and the bare `sectionListRenderer` shape) and returns each `musicCarouselShelfRenderer` as a titled `HomeSection`, reusing `parseHomeShelf` so the visual treatment matches Home rails. Previously `related(browseId:)` flattened everything via `scanForMediaItems` and dropped the shelf titles on the floor — the live / acoustic / cover variants were in the response all along. PlayerBridge gained a `relatedSections: [HomeSection]` field (blocked-artist filtered just like `related`), reset on every track change, and `loadRelatedIfNeeded()` now fetches both flat items and sections in parallel via `async let` so the Related tab opens in the same single round-trip. NowPlayingView's `relatedContent` looks up the shelf whose title contains "other version" / "other performance" (case-insensitive, locale-tolerant) and renders it as a compact 110×110 horizontal rail above the flat related queue, with TrackContextMenu on each tile so users can queue a variant without leaving Now Playing. Added `InnerTubeParserTests.testParseRelatedSectionsSurfacesTitledShelves` + `testParseRelatedSectionsEmptyOnUnknownShape` to guard the parser against renderer drift.
- [x] **A6. Floating Mini Player window** — SwiftUI WindowGroup with floating level, resizable down to a compact tile, hover-reveal controls. Cmd-Opt-M opens.
  - note: Reworked `FloatingMiniPlayerView` from a fixed 360×70 strip into a resizable artwork tile. Artwork fills the window (`AsyncImage` + `aspectRatio(.fill)` + `clipped`); a bottom gradient hosts title/subtitle so the resting state stays glanceable. Transport row (prev / play / next) is hover-reveal — `@State private var isHovering` toggled by `.onHover`, scrim + buttons fade in via `.opacity` + `.animation(.easeInOut(duration: 0.15))`, and `.allowsHitTesting(isHovering)` prevents the invisible scrim from intercepting window-drag gestures when the pointer is outside. Tile carries `frame(minWidth: 280, minHeight: 140)` to enforce the spec's compact minimum. RiffApp's `Window` scene swapped `defaultSize(360×70)` → `defaultSize(320×180)` and `windowResizability(.contentSize)` → `.contentMinSize` so the user can grow the tile freely but not shrink below the 280×140 minimum. ⌥⌘M shortcut (`MiniPlayerMenuItem`) + `.floating` level + all-spaces collection behavior + right-click `TrackContextMenu` all preserved from the previous incarnation. No second WKWebView — the window is a pure SwiftUI surface onto the same `env.player`, satisfying the architectural rule.
- [x] **A7. Menu bar mini player** — MenuBarExtra with artwork, title, scrubber, transport. Quintessential macOS power-user feature.
  - note: Added a `MenuBarExtra` scene to `RiffApp.swift` (alongside the main `WindowGroup` and the A6 floating `Window("Mini Player")`) plus a new `Sources/UI/Player/MenuBarMiniPlayer.swift` housing the popover view (`MenuBarMiniPlayer`) and the menu-bar glyph (`MenuBarMiniPlayerLabel`). Popover is fixed-width 280pt: 44×44 artwork tile + title/subtitle, a Theme.red progress capsule with `m:ss` elapsed / `-m:ss` remaining timestamps (monospaced digits so they don't shove the bar around), prev / play-pause / next transport row, and a footer with "Open Mini Player" (`openWindow(id: "mini-player")`, ⌥⌘M to match A6) + "Quit Riff" (⌘Q). Reads from the same `env.player` — no second WKWebView, honors the architectural rule. Empty state stays intentional: artwork shows a `music.note` SF Symbol, transport buttons disable via `!env.player.hasTrack`, progress strip collapses. The menu-bar label is an SF Symbol that mirrors transport state (`play.fill` playing, `pause.fill` paused, `music.note` idle) so users can read playback state at a glance without opening the popover. Style is `.menuBarExtraStyle(.window)` for the rich popover, not the menu style. Runtime-toggleable via a new `AppEnvironment.menuBarExtraEnabled` (defaults true, persisted under `ui.menuBarExtraEnabled` in UserDefaults, `object(forKey:)` read so first-launch absence is distinguishable from explicit-false), bound through a hand-rolled `Binding<Bool>` into the scene's `isInserted:` so the menu bar item appears/disappears live without relaunch. Added an "Interface" section to `SettingsView` between Playback and Library access with the toggle + a one-paragraph rationale.
- [x] **A8. Block-artist button inline on Artist detail page** — already in Settings and context menu; surfacing inline saves two clicks.
  - note: Added an inline `blockArtistButton(page:)` helper to `DetailView` and conditionally render it in the header action row alongside Play / Shuffle when `item.kind == .artist`. State-aware: SF Symbol + label flip between `hand.thumbsdown` "Don't Recommend" and `hand.thumbsup` "Recommend Again" based on `env.isBlocked(artistId:)`, so the same control is also the unblock affordance — no Settings round-trip to undo a misclick. Uses `page.title` (with `item.title` fallback) for the captured human-readable name to avoid the stale-subtitle problem when arriving via "Go to artist" from a song row where the artist segment may have been truncated. `.help(...)` tooltip explains the across-Home/Search/radio scope and points to Settings → Library for bulk management. Mirrors `env.blockArtist(id:name:)` / `env.unblockArtist(id:)` — same code paths the context menu and Settings already exercise, no new model surface.
- [x] **A9. Mood / activity chip row on Home** — render the mood chips InnerTube already returns ("Focus", "Workout", "Sleep", "Commute") as a horizontal strip above Home carousels.
  - note: Added `InnerTubeClient.HomeChip` (Sendable + Hashable, with `label` + opaque `params` blob) and a `HomeFeed` envelope so `browseHome()` returns both the rails AND the chip cloud above them in a single round-trip — chips arrive in the same `FEmusic_home` response we were already fetching, the parser just wasn't reading them. New static `parseHomeChips(_:)` walks the standard `singleColumnBrowseResultsRenderer…sectionListRenderer.header.chipCloudRenderer.chips` path with fallbacks to `musicChipCloudRenderer` and the bare top-level shape; drops chips with empty labels or missing `browseEndpoint.params` so the row never renders inert pills. New `browseHomeFiltered(chipParams:)` re-fetches Home scoped to a chip's `params` blob (no continuation tokens — chip-scoped Home is curated and finite by design). HomeView grew a `MoodChipRow` above the carousels: horizontal-scroll capsule pills using `Theme.red` as the active fill (no chromatic accent leaks anywhere else, matching the brand restraint of the rest of the app), white-on-red text when active and white-on-translucent-surface when not, hover-state opacity bump, `.help(...)` tooltip per state, and `.scrollClipDisabled()` so the focus ring doesn't get cropped at the row edges. Tap a chip → re-fetch Home filtered; tap the active chip again → restore default Home. The chip row itself never re-fetches across filter changes — chips are stable across the response variants we observe — so flipping vibes only blinks the rails. Honored the architectural rule trivially (no WebView surface change). Added `InnerTubeParserTests.testParseHomeChipsStandardShape` (verifies label + params extraction AND that empty-label / missing-params chips are dropped) + `testParseHomeChipsEmptyOnUnknownShape` (guards against parser crashes on renderer drift).

### Tier B: Ship next month (parity + macOS-native moves)

- [x] **B1. Always-on Stats / Replay dashboard** — keep Recap's stats math but expose year-round with 7-day, 30-day, 90-day views. Apple Music does this.
  - note: Promoted the existing year-end-flavored Recap sheet into a permanent Stats surface. Added `RecapWindow` (7d / 30d / 90d / all-time) above the existing `RecapStats.compute(from: [MediaItem])` math via a new `compute(from: [PlayedEntry], window:, now:)` overload that filters by recency before delegating to the legacy entry point — no math re-derived, the year-end shape and the bounded shape return identical numbers when both see the same items. Persistence model gained timestamps: introduced `PlayedEntry { item, playedAt }` and bumped the on-disk journal from `player.history.v2` (`[MediaItem]`) to `player.history.v3` (`[PlayedEntry]`) with an in-line migration on load — pre-B1 entries get `Date.distantPast` so they show under "All time" (we have the item, we just don't know when) but are correctly excluded from every bounded window. `QueueManager.archive(_:at:)` records `Date()` synchronously alongside the item; cap bumped from 50 → 500 so 30-day / 90-day windows actually have a meaningful sample on an active listener (~25 KB encoded, microseconds per write). `playedHistory: [MediaItem]` is now a derived view of `playedEntries`, so every legacy caller (NowPlaying's "Familiar" chip, Up-Next "Recently played" rows, the original test suite) keeps working without changes. RecapView gained a segmented window picker pinned above the body, a window-aware empty state ("Nothing in this window — try a wider window" with a one-tap "Switch to All time" button when the user has any history outside the active window), and a window-aware intro headline. Help-menu launcher renamed from "Your Riff Highlights" → "Stats…" with ⌘⇧Y; it's the same sheet, same dismiss path, so the existing in-app entry points keep working. Test sentinel updated to guard both v2 and v3 keys against test-suite pollution. Added 6 new tests covering: `.allTime` matching legacy `compute`, 7d cutoff excluding older entries, 30d boundary inclusivity, 90d windowing, `.distantPast` entries hidden from bounded windows but visible in all-time, and empty-entries safety across every window.
- [x] **B2. Live time-synced lyrics (karaoke-style)** — word-by-word highlight (not just line-by-line), tap to seek. Apple Music's Sing feature, minus the vocal-attenuation we cannot do.
  - note: New `Sources/UI/Player/LyricsKaraoke.swift` houses pure-logic helpers (`activeLyricIndex`, `lineDurationMs`, `lineProgress`) plus a `KaraokeLineView` that renders the active line via a single `Text` with a two-stop `LinearGradient` `foregroundStyle` — full white left of `progress`, 40% white right of it, 1% feather between for a glide instead of a wipe. Single-Text-with-gradient (vs. an HStack of word views) preserves native `Text` wrapping so long lyric lines that wrap to two visual rows still fill correctly. Per-line duration is derived from the next timed line's `startMs` (skipping empty-interlude ♪ lines that share a startMs), floored at 250ms to guard the malformed-response edge case where YT returns two lines at the same startMs; tail line falls back to a 3.5s default. NowPlayingView's `syncedLyrics` was replaced with a `TimelineView(.periodic(by: 1/30))` driven view that re-anchors `(elapsedMs, at: Date)` on every `env.player.elapsed` change and extrapolates locally between the bridge's ~1Hz polls — capped at 2s of drift so a stalled poll doesn't sweep past lines. While paused, the fill freezes at the anchor so it doesn't keep advancing through the line. Every line is wrapped in `.contentShape(Rectangle()).onTapGesture` that calls a new `seekToLyric(at:)` — converts the line's `startMs` to a 0…1 fraction over `env.player.duration` and calls the existing `PlayerBridge.seek(to:)` (no new JS bridge surface needed). `.help("Jump to this line")` tooltip on each row makes the affordance discoverable. Added 10 tests in `Tests/LyricsKaraokeTests.swift` covering active-index resolution (negative before first line, exact-match boundaries, empty), line-duration derivation (next-line, tail fallback, dup-startMs floor), `lineProgress` clamping (zero before start, zero at exact-start to prevent flash-of-completed-fill on seek, midway accuracy, clamped past end, zero-on-missing-startMs), and `splitWords` whitespace preservation. Architectural rule untouched (no new WebView surface, no new audio path).
- [x] **B3. Lyric translation + pronunciation (BYO-LLM)** — toggle on lyrics tab; Claude translates each line to a user-selected language, optional romanized pronunciation below. Cache per videoId.
  - note: New `Sources/AI/LyricsTranslator.swift` is a `@MainActor @Observable` class wrapping the existing `LLMProvider` plumbing (Tier 2 #12) with a per-`(videoId, language)` in-memory cache + in-flight task table that coalesces concurrent translate calls so a fast toggle-off + toggle-on never fires two requests. System prompt asks the model for a JSON array of `{translated, pronunciation}` objects, one per input line, with `pronunciation` populated ONLY when the source is non-Latin script (Devanagari, Telugu, kanji/kana, hangul, hanzi, Cyrillic, Arabic) — the model decides per-line, the view just renders whatever shape lands. `LyricsTranslator.parse` is a tolerant decoder modeled on `QueueBuilder.parseSuggestions`: strips ```json fences, locates the first balanced top-level `[..]` via a string-aware depth tracker (so `\"` and `]` inside string literals don't terminate the slice early), and JSON-decodes; bare-string entries and non-object/non-string entries are silently coerced to empty placeholders so per-line index alignment with the original lyrics never breaks. Output is right-sized to the input line count (padded with empty entries when the model drops lines, truncated when it doubles up) so the renderer can index by position without bounds checks. `AppEnvironment` gained `lyricsTranslator` (lazy + `@ObservationIgnored`, same pattern as `llmProvider`), `translationLanguage` (persisted under `lyrics.translationLanguage`, defaults to "English"), and `lyricsTranslationEnabled` (persisted under `lyrics.translationEnabled`); both load via `object(forKey:)` on init so an absent default doesn't shadow the property default. Settings → AI features grew a language picker with the 9 spec presets (English, Spanish, French, German, Japanese, Korean, Chinese, Hindi, Telugu) plus an "Other…" sentinel that surfaces a free-text TextField + "Apply" button for anything off-list (the LLM accepts any reasonable language name). Settings' "Clear" key button now also calls `lyricsTranslator.clearCache()` so a fresh key doesn't inherit translations from the previous one. NowPlayingView's lyrics tab grew a `translationToolbar` rendered next to "Create card": when `env.hasLLMAPIKey == true`, a compact switch toggle labeled "Translate (<lang>)" with a spinner sibling for the in-flight state; when no key is configured, the toggle is replaced by a borderless "Translate — configure API key in Settings" button that raises `isSettingsSheetPresented` — gated exactly per the B3 spec. Translations render as italicized `Theme.red.opacity(0.9)` lines indented 4pt under each source line, with the optional pronunciation rendered above the translation in 11pt monospaced 50%-white (clearly subordinate to the source). Both the synced (karaoke) line view and the plain-text fallback share a single `translatedSubtitle(at:)` builder so both surfaces render identically. `fireTranslateIfNeeded()` resets translation state and re-fires on changes to track videoId, target language, toggle, or lyric-line count (lyrics often land async after the tab opens); the in-flight Task discards its result if the user has skipped tracks or changed language while the request was in flight so a stale translation never lands on the wrong song. Tests in `Tests/LyricsTranslatorTests.swift` cover the parser's happy path, markdown-fence stripping, prose tolerance, right-sizing in both directions, bare-string-entry tolerance, empty-pronunciation coercion to nil, non-object/non-string entry skipping, throw-on-no-array, throw-on-unclosed-bracket, escaped-quote safety, and brackets-in-strings safety. Zero new SPM dependencies, strict Swift 6 concurrency preserved (translator is `@MainActor`, captured factories are `@MainActor`-typed). Architectural rule untouched — no WebView surface change.
- [x] **B4. X-Ray context cards (BYO-LLM)** — swipe-up panel on Now Playing; Claude generates cards for lyric references (people, places, events). No licensed data needed.
  - note: New `Sources/AI/XRayCardsService.swift` is a `@MainActor` class mirroring `LyricsTranslator` (B3) — same `lazy var` + `@ObservationIgnored` pattern in `AppEnvironment`, in-memory cache keyed by videoId, in-flight task table coalescing concurrent calls, and a tolerant JSON-array parser (markdown-fence stripping + string-aware balanced-bracket extraction) lifted from the same shape as `LyricsTranslator.parse` and `QueueBuilder.parseSuggestions`. Defines a closed `CardKind` enum (`people` / `place` / `event` / `sample` / `trivia`) with per-kind SF Symbols + uppercase tag labels so the renderer never string-matches on freeform model output — unknown / missing `kind` values fall back to `.trivia` rather than dropping the card. System prompt asks the model for 3-6 cards covering people / places / era / samples / trivia as `{"title", "body", "kind"}` objects, explicitly forbids fabrication and defamatory speculation about living people, and tells it to omit a category rather than invent content. Lyric input is capped at 60 lines (`maxLyricLines`) so very long songs stay token-bounded; when lyrics aren't loaded the model answers from title + artist alone (the era / trivia cards usually don't need verse content). Parser silently drops cards with both an empty title AND empty body but allows title-only cards (the body row just hides), and coerces non-object array entries to safe skips so a partial response still renders the cards that parsed cleanly. NowPlayingView gained a fourth `BottomTab.context` case alongside Up Next / Lyrics / Related, rendered as a vertical stack of cards with subtle 12%-white hairline dividers between them — magazine-style, no chromatic accents beyond `Theme.red` on the kind icon. Each card shows an uppercase kind tag (10pt, 55%-white, 0.8pt tracking), the title (15pt semibold), and the body (12pt, 78%-white, 2pt line-spacing). When `env.hasLLMAPIKey == false`, the tab renders a configure-prompt with an "Open Settings" button instead of firing the request — gated exactly per the B4 spec. Errors render with a "Try again" affordance that bypasses any prior failure-cache state via a `force:` flag. View state resets on track change and re-fires only if the Context tab is currently open (no background fetch when the user never opens the panel). Settings' "Clear" key button also calls `xrayCardsService.clearCache()` alongside the lyrics-translator cache so a fresh key starts clean. Added 13 tests in `Tests/XRayCardsServiceTests.swift` covering the happy path, markdown-fence stripping, prose tolerance, unknown / missing kind fallback to `.trivia`, empty title+body dropping, title-only survival, non-object-entry skipping, case-insensitive kind matching, throw-on-missing-array, throw-on-unclosed-bracket, escaped-quote string safety, and brackets-inside-strings safety. Zero new SPM dependencies, strict Swift 6 concurrency preserved (service is `@MainActor`, factories `@MainActor`-typed). Architectural rule untouched — no WebView surface change.
- [x] **B5. Smart Shuffle with plus badge** — when shuffle is on, every Nth slot in upNext is a recommendation from /related, marked with a plus badge. Spotify's pattern.
  - note: New `Sources/Audio/SmartShuffle.swift` houses a pure `merge(base:pool:every:protectedIds:)` interleaver — emits one pool item before every Nth base item, dedupes against `base` ids AND the protected set (currentTrack + userQueuedIds) so the current track and any "Play next" items never get replaced by a suggestion, clamps `every >= 2` so a misconfig can't carpet-bomb the queue. Pool exhaustion is a graceful tail (remaining base passes through unmodified) — we deliberately do NOT loop the pool, repeating the same recommendation every 4 slots would erode trust faster than no suggestion at all. PlayerBridge gained `smartShuffleEnabled: Bool` (persisted under `player.smartShuffle`, separate from `player.shuffle` so a user can keep their preference around a shuffle off/on cycle), `smartShuffleInjectedIds: Set<String>` (observable so the QueueRow can render its badge without an extra MediaItem field), `toggleSmartShuffle()`, and `applySmartShuffleToCurrentQueue()` — the idempotent re-merge that the toggle handlers + /next response handler + /related load handler all call. Gating: injections only land when BOTH `smartShuffleEnabled` AND `shuffleEnabled` are on (matches Spotify's behavior); flipping either off strips injected rows from upNext on the spot without forcing a /next refresh. Toggling Smart Shuffle on also kicks `loadRelatedIfNeeded` so users see "+" rows without having to open the Related tab first. NowPlayingView's shuffle button uses `shuffle.circle.fill` when both flags are on (so the active-active state is visually distinct from plain shuffle), gains a `.contextMenu` with an Enable/Disable Smart Shuffle item + a one-line explanation, and a `shuffleHelpText` computed property that surfaces both states in the hover hint. `queueRow(item:isCurrent:)` reads `env.player.smartShuffleInjectedIds` and renders a 16×16 Theme.red filled circle with a white `plus` glyph at the trailing edge, with both `.help` and `accessibilityLabel` set to "Smart Shuffle suggestion". Strict Swift 6 preserved (`SmartShuffle` is a plain `enum` namespace, all mutation paths land on `@MainActor` via PlayerBridge). Zero new SPM dependencies — uses the existing /related fetch surface. Architectural rule untouched (no WebView surface change). Added 10 tests in `Tests/SmartShuffleTests.swift` covering empty-base / empty-pool short-circuits, injection cadence at every=3 and every=4, pool exhaustion (no looping), pool members already in base getting skipped, protected ids never injected, pool deduplication, `every` clamped at 2, and base-order preservation under interleave.
- [x] **B6. Pinned Library + smart filters** — drag-pin items to top of Library, treat Liked Songs as a real filterable playlist (sort by play count, recently added, last played).
  - note: New `Sources/UI/Library/LibrarySorting.swift` houses three pure helpers (`playCounts(from:)`, `lastPlayed(from:)`, `partitionPinned(_:pinned:)`) so the sort + pin logic is testable without spinning up an `AppEnvironment` or the SwiftUI view tree. Counts and last-played timestamps are derived from `PlayerBridge.playedEntries` — InnerTube doesn't return per-track play stats on the Liked Songs browse response, and the local timestamped journal (added in B1) is the only source the user actually trusts. `partitionPinned` is a stable partition (NOT a re-sort by pin status) so within both the pinned head and the unpinned tail the caller's chosen sort order is preserved — pinning is layered ON TOP of recently-added / A-Z / most-played, not in conflict with it. AppEnvironment gained `pinnedLibraryIds: Set<String>` (persisted as a sorted `[String]` under `library.pinned` because `Set` isn't a PLIST type), `isPinned(_:)`, and `togglePinned(id:)` — pin state is cross-section by design (an id pinned anywhere stays pinned everywhere) but the menu affordance is gated to Library context. LibraryView's `SortOrder` gained `.playCount` ("Most played") and `.lastPlayed` ("Last played") with a `requiresPlayHistory` flag so the dropdown filters them out for non-Liked sections where per-row play counts have no honest definition (a playlist row's "play count" is undefined here — would we count tracks-played-from-it, opens, both? Better not to lie); section-switch task resets the sort to `.recentlyAdded` if the active sort isn't in the new section's `availableSorts`. Both new sorts tie-break alphabetically so SwiftUI doesn't shuffle equally-played rows on every play-count tick. `displayedItems` order of operations is filter → sort → `partitionPinned` so pinning always wins regardless of the chosen sort. `ThumbnailButton` gained `showPinAction: Bool = false`; LibraryView's grid passes `true`, every other surface (Home / Search / Explore / Now Playing) gets the default `false` so the menu isn't cluttered with a pin affordance that has no destination. The action appears in both menu variants — `TrackContextMenu` (Liked songs, episodes) and the inline `else`-branch context menu (playlists / albums / artists / podcasts) — with the label flipping between "Pin to top" and "Unpin from top" based on `env.isPinned(id:)`. Pinned tiles get a small `pin.fill` glyph in a Theme.red filled circle at the top-left corner with a "Pinned — right-click to unpin" tooltip so the visual cue + the undo affordance are both discoverable without opening Settings. Architectural rule untouched (no WebView surface change). Zero new SPM dependencies. Added 8 tests in `Tests/LibrarySortingTests.swift` covering: play-counts aggregation by id with absent ids returning nil, empty-journal safety on both maps, `lastPlayed` keeping the max timestamp per id (later entries don't get overwritten by earlier ones with the same id), `partitionPinned` floating pinned to head while preserving relative input order, empty-pin-set as a no-op, all-pinned as a no-op, stale-id tolerance (pin set referencing items no longer in the input doesn't invent rows), and the stable-partition contract on a 5-item input.
- [x] **B7. Smart sorts on every list surface** — Recently Added, Last Played, Play Count, A-Z, Duration menus, sticky per surface.
  - note: Extended B6's `Sources/UI/Library/LibrarySorting.swift` with a shared `TrackSortOrder` enum (`original` / `aToZ` / `zToA` / `playCount` / `lastPlayed` / `durationShortest` / `durationLongest`) plus a pure `sortTracks(_:by:entries:)` helper — all sort math lives in one place so DetailView / SearchView / Library can't drift. `.original` morphs its label per surface ("Album order" / "Playlist order" / "Relevance" / "Original order") via `displayName(for: TrackSortSurface)` so the menu copy reads naturally on each surface; non-`.original` cases share one label across surfaces. Missing-duration items sort to the tail under both duration orders (Int.max sentinel for ascending, -1 for descending), keeping the comparator stable rather than randomly shuffling items YT didn't surface a `lengthText` for. Every sort tie-breaks alphabetically on title so SwiftUI doesn't permute equally-played / equally-long rows on every render. Sort is a pure function — input array is never mutated (test guards this). New `Sources/UI/Components/TrackSortMenu.swift` is the single shared SwiftUI pill used on every track-list surface. Matches LibraryView's capsule pill visual so all four surfaces feel like the same control; the menu owns its own per-surface UserDefaults hydration via a `.task(id: persistenceKey)` that re-reads from defaults whenever the key changes (e.g. user navigates album A → album B; each gets its own remembered sort). Passing `nil` for `persistenceKey` makes the menu in-memory-only — leaves the door open for the Up Next preview-mode mentioned in the heads-up without code change. `DetailView` gained a `@State private var trackSort` + helpers `trackSortPersistenceKey` (`tracks.sort.{item.id}`, scoped per album/playlist/podcast so each entity remembers its own sort), `trackSortSurface` (album/playlist/generic per kind), `showsTrackSortMenu` (hidden on artist pages — YT's top-tracks rail is already curated — and when tracks are empty), and `availableTrackSorts` (prunes duration sorts off podcasts since episode runtimes vary by hours rather than minutes). `playAll(_:)` honors the sort order so "Play" on a tracklist sorted by "Most played" queues most-played-first instead of album order, matching Spotify / Apple Music behaviour. `SearchView` gained a `@State private var sort` + per-filter sort menu under `search.sort.{filter}`. Hidden under `.all` (heterogeneous result set — duration / play-count sort would compare apples to oranges), and `displayedResults` skips sorting under `.all` to preserve YT's relevance ordering. `availableSortOrders` is filter-aware: songs / podcasts surface the full set; albums / playlists / artists drop duration and play-history sorts (the search response doesn't carry album-tile durations, and we don't journal album-tile plays). `.onChange(of: filter)` resets sort to `.original` when the previously-selected order isn't in the new filter's available list; the menu's own `.task(id:)` then re-hydrates from the new key right after, restoring any previously-remembered choice on the new filter. Zero new SPM dependencies; strict Swift 6 concurrency preserved (`TrackSortOrder` / `TrackSortSurface` are `Sendable`). Architectural rule untouched (no WebView surface change). Added 13 tests to `Tests/LibrarySortingTests.swift` covering: `.original` is identity, case-insensitive A-Z + Z-A, play-count desc with alphabetical tie-break, unplayed-as-zero under play-count, last-played most-recent-first with `.distantPast` tail, both duration orders, missing-duration sinks to tail under both orders, sort is non-mutating, `requiresPlayHistory` flag correctness, and per-surface label adaptation for `.original`.
- [x] **B8. Crossfade between tracks** — JS-side cross-fade by overlapping volume ramp on track-end. 80% of Apple's AutoMix quality without needing raw audio.
  - note: Added a `__riffXfade` module to `Resources/player-bridge.js` that owns a multiplicative crossfade factor in [0,1] applied to the user's cached volume on every `<video>.volume` write — so the user's `setVolume` calls never fight an in-flight ramp. `window.musicBridge.setVolume(level)` now routes through the module (caches `userVolume`, multiplies by `factor`, writes the product) instead of writing `<video>.volume` directly; this means the existing Tier 2 #8 normalization GainNode, the A3 sleep-timer fade, and crossfade all compose correctly — every scalar multiplies through the same path. Tail ramp lives in the existing `timeupdate` handler: when `duration - currentTime < seconds`, factor linearly ramps `(duration - currentTime) / seconds` from 1.0 → 0.0. Scrub-backwards-out-of-window restores factor to 1.0 and clears the `tailFading` flag so we don't start a phantom head ramp on the next track change. Head ramp fires on the next `videodatachange` after `videoIdChanged === true` (same dedup gate normalization uses, so we don't double-fire on the late-arriving metadata re-emit): a 50ms setInterval wall-clock ramps factor 0.0 → 1.0 over `seconds`, then self-cancels. Both halves use the same N — for a 4s crossfade you hear 4s of fade-out followed by 4s of fade-in on the new track. New JS surface `setCrossfadeSeconds(n)` (0 / 2 / 4 / 6 / 8 supported); setting 0 immediately cancels any in-flight head ramp and restores factor=1.0 so the user can't be stranded with a half-faded level. Swift side: `PlayerBridge.crossfadeSeconds` (persisted under `player.crossfadeSeconds`, `object(forKey:)` read so first-launch absence stays a clean 0 rather than re-writing the default), `PlayerBridge.crossfadePresets = [0, 2, 4, 6, 8]` (Apple Music's default is ~6s), and a `clampCrossfade(_:)` helper that snaps arbitrary doubles to the nearest preset and guards against `.nan` / `.infinity` (both → 0) so a corrupted plist can't leave the user with an unkillable crossfade. The `ready` flush now re-pushes the persisted crossfade duration alongside volume / rate / repeat / normalization so SPA reloads don't lose the setting. Settings → Playback grew a "Crossfade" row between Volume normalization and the section divider — `.menu`-style Picker with Off / 2s / 4s / 6s / 8s entries, 90pt fixed width, label hidden, with copy that flags the volume-only limitation up-front so users know not to expect beat-matching. Architectural rule untouched (no WebView surface change — the entire ramp is inaudible-to-debug audio). Zero new SPM dependencies. Added `CrossfadeSettingTests` in `Tests/QueueAndResolverTests.swift` covering the preset list, exact-match passthrough, off-preset snapping with documented tie-break behavior, and `.nan` / ±`.infinity` / negative / overshoot guarding. JS side smoke-tested out-of-band via a 9-check node harness (factor at remaining=2/4, scrubback reset, disable-cancels-ramp, disabled-is-noop, snap-to-1 on un-faded track change, setVolume userVolume caching, factor at remaining=1/4 and remaining=0).
- [x] **B9. Notification Center Now Playing widget** — WidgetKit ControlWidget.
  - note: Shipped a real WidgetKit extension named RiffWidget rather than a ControlWidget so the widget lands in macOS Notification Center. New app-extension target in project.yml with the widget extension point identifier plus a CFBundlePackageType of XPC. Both targets carry the shared App Group entitlement so host and widget share a UserDefaults suite plus a per-app-group container directory for cached artwork. Three files in Sources/Widgets/ are compiled into BOTH targets so AppIntent types serialize identically across the process boundary: NowPlayingSnapshot.swift is a Codable plus Sendable wire-format with a projectedElapsed helper that extrapolates progress between writer ticks and clamps to the zero-to-duration band, gating extrapolation on isPlaying so paused snapshots freeze visually; RiffWidgetIntents.swift defines three AppIntents — Toggle, Next, Previous — that enqueue a WidgetCommand into the shared suite via WidgetCommandStore and set openAppWhenRun true so the host foregrounds. A fourth app-only NowPlayingSnapshotWriter.swift owns rate-limiting: squelch progress-only ticks when the elapsed delta is under 10s AND the last write was under 10s ago, so a 250ms onUpdate cadence doesn't spam the App Group plist or WidgetKit's reload pipeline. The writer also calls WidgetCenter.shared.reloadTimelines on every material write and does a JPG-resize-to-256x256 artwork copy into the App Group container so the widget reads it by file URL — no network in the widget render path, keeping it inside WidgetKit's per-render budget. AppEnvironment.player.onUpdate now mirrors track payloads into the writer and calls clear when currentTrack is nil so the widget wipes to its placeholder instead of advertising stale state. Widget-to-app transport runs through WidgetCommandStore enqueue and dequeue with a 30s freshness gate (older commands assumed orphaned — prevents play-music-ten-minutes-later edge cases when the app was force-quit), drained by AppEnvironment on didBecomeActive plus once on init. NowPlayingWidget.swift ships a StaticConfiguration supporting systemSmall (artwork tile with overlaid title gradient plus a widget URL deep link) and systemMedium (artwork plus title plus full transport row whose buttons disable when there's no snapshot). Transport uses SwiftUI's Button-intent initializer, macOS 14+. Strict Swift 6 preserved — writer is MainActor, snapshot is Sendable, the artwork Task hop stays MainActor since NSImage drawing is main-thread-only. Zero new SPM dependencies. Architectural rule untouched — widget reads a static snapshot plus cached JPG, never a WKWebView. Added Tests/NowPlayingSnapshotTests.swift with 8 tests covering paused-doesn't-extrapolate, playing-extrapolates-linearly, duration clamping, clock-skew safety, missing and empty thumbnail string handling, Codable round-trip, and WidgetCommand rawvalue stability. Contributors must re-run xcodegen generate after pulling (existing project convention; Xcode project is gitignored).
- [x] **B10. AirPlay / output device picker** — AVRoutePicker on the transport row.
  - note: Shipped the pragmatic deep-link approach documented in the Ralph heads-up. `AVRoutePickerView` is iOS-only, and a CoreAudio-enumerated `NSPopUpButton` wouldn't surface AirPlay receivers (which only the Sound preference pane fully discovers). New `audioOutputButton` on NowPlaying's transport row sits between `playbackRateMenu` and `sleepTimerMenu` — an `airplayaudio` SF Symbol (40×40 hit target, same sizing as the other transport controls, `.white.opacity(0.75)` for consistency with the rest of the secondary controls). Tap opens `x-apple.systempreferences:com.apple.preference.sound?Output` via `NSWorkspace.shared.open`, which lands directly on Sound → Output where AirPlay devices, Bluetooth, and built-in speakers all live in one consistent picker the user already trusts. `openSoundOutputSettings()` checks `NSWorkspace.open`'s `Bool` return and falls back to opening `/System/Library/PreferencePanes/Sound.prefPane` directly if the URL handler isn't registered (older macOS releases). `.help("Audio output (opens System Settings → Sound)")` tooltip documents the OS-routing limitation up-front so users know not to expect an inline route picker. Added an `import AppKit` to NowPlayingView.swift (was SwiftUI-only) for the `NSWorkspace` symbol. Architectural rule untouched (no WebView surface change — the OS owns the audio routing layer below `<video>.volume`). Zero new SPM dependencies. The doc-comment on `audioOutputButton` records the design rationale and the macOS-vs-iOS API gap so a future contributor doesn't try to swap in `AVRoutePickerView` and discover it doesn't exist on macOS.
- [x] **B11. Endless scroll on Home / Library / Search** — InnerTube continuation tokens are supported; we currently single-shot every browse.
  - note: Shipped end-to-end continuation support for Search — the surface the heads-up flagged as having the cleanest data shape (flat `[MediaItem]` list, single `musicShelfRenderer` carrying its own `continuations` field on every kind-scoped filter). New `InnerTubeClient.SearchPage` Sendable struct (`items: [MediaItem]`, `continuation: String?`) is the return type of a new `searchPaged(query:filter:)`; the legacy `search(query:filter:)` is preserved as a thin one-liner wrapper returning `.items` so the three external callers (QueueBuilderSheet, RiffIntents play-song / play-artist) need zero changes. New `searchContinue(token:)` posts `{"continuation": token}` against `/search` and parses the response under `continuationContents.musicShelfContinuation` (with fallbacks for `gridContinuation` and `musicPlaylistShelfContinuation` shapes) — synthesizes a one-element shelf array wrapping the continuation envelope as a `musicShelfRenderer` so the existing `parseSearchShelves` helper drives both the initial AND continuation paths from one place (drift between the two is the main risk when YT rotates renderer keys, so single-parser-for-both is the load-bearing invariant). Continuation-token lookup is centralized in a new `findSearchContinuationToken(in:)` that walks `musicShelfRenderer` / `gridRenderer` / `musicPlaylistShelfRenderer` for `continuations[0].nextContinuationData.continuation` (with reload-shape fallback) and treats the empty string as no-more so placeholder tokens can't trigger an infinite empty-page loop. `.all` searches deliberately return nil — the heterogeneous response carries no top-level continuation and continuation under `.all` would mix kinds unpredictably; users wanting endless results pick a kind-scoped filter. SearchView gained `nextPageToken: String?`, `loadingMore: Bool` (re-entrancy guard so scroll-jitter at the bottom doesn't fan out into parallel fetches), and `loadMoreError: String?` (last-continuation-failed banner — never clobbers `results` so the user keeps their scroll position). `debouncedRunSearch` swapped to `searchPaged` and resets all three on every new query; new `loadMoreIfNeeded()` captures `query` + `filter` before the await and discards the result if the user changed inputs mid-flight (stale-result safety). De-dupes by id against existing rows on append — YT has been observed returning the same row across pages, and SwiftUI's `ForEach` blows up on duplicate ids. The trigger is a 1pt invisible `Color.clear` sentinel rendered as the last row of the LazyVStack — its `.onAppear` only fires when SwiftUI materializes it (i.e., the user has scrolled within ~one viewport of the bottom), which is the cheap-correct way to wire endless scroll without a custom scroll-position observer. While `loadingMore`, a small "Loading more…" ProgressView appears under the sentinel; on failure a "Retry" button shows the error and re-fires `loadMoreIfNeeded()`. Zero new SPM dependencies; strict Swift 6 preserved (`SearchPage` is `Sendable`, `searchContinue` is the same `@unchecked Sendable`-on-the-client surface as every other endpoint). Architectural rule untouched (no WebView surface change). Added 5 new tests in `InnerTubeParserTests.swift` covering the happy path (`nextContinuationData` resolves), the legacy `reloadContinuationData` shape, missing-continuations returns nil (so `.all` doesn't hallucinate a token), empty-string-continuation treated as nil (placeholder safety), and `parseSearchShelves` flattening list rows from a `musicShelfRenderer` shape (the same parser path the continuation envelope is synthesized to hit, guarding the initial-vs-continuation drift invariant). Home and Library are deliberately deferred to a follow-up Ralph iteration — Home's shelf-of-rows continuation has a different shape (`sectionListContinuation` already used by `browseHome`, but with shelf-level vs section-level token semantics worth designing carefully) and Library's signed-in shape adds wrapper variance — picking ONE surface end-to-end is what the heads-up asked for, and Search ships in a single small diff with the cleanest test surface.

### Tier C: Differentiators (longer-form / experimental)

- [ ] **C1. AI DJ voice narrator (BYO-LLM + TTS)** — between tracks, Claude generates 8-15s commentary, AVSpeechSynthesizer voices it, PlayerBridge ducks the next track's intro.
- [ ] **C2. Daylist with flavor-text titles** — auto-refreshing playlist updates 4-6x per day with a Claude-authored evocative title. The title IS the magic.
- [ ] **C3. Smart Sections (custom user-defined Home rows)** — user writes a prompt ("post-rock albums I haven't played in 6 months"), Claude compiles to a query over local history plus InnerTube search, renders as a permanent Home row. Marvis Pro's killer feature, un-replicable by the big four.
- [ ] **C4. "Explain this queue" / "Ask about your year" panel** — button in Up Next header; Claude reads the queue and writes a 2-3 sentence vibe summary. Also a chat over local play history.
- [ ] **C5. Magazine-style Artist pages (Roon-inspired)** — BYO-LLM-generated artist bio, long-form layout with discography arc, top tracks rail.
- [ ] **C6. Track Credits drill-through (Tidal-inspired)** — producer, engineer, songwriter chips on each track; tap to see every other track that person touched.
- [ ] **C7. Album Collections (Doppler-inspired)** — user-grouped album bundles separate from playlists.
- [ ] **C8. Wrapped-style cinematic Stats stories** — full-screen tap-to-advance cards of listening data with motion + aura colors. Annual viral moment, re-usable for monthly recaps.

### Tier D: Open-ecosystem / power-user

- [x] **D1. last.fm + ListenBrainz scrobbling** — optional sign-in via API key. Frees users from Riff's data silo. Distinctive "this respects you" signal.
  - note: New `Sources/Data/Scrobblers/` directory with three files. `Scrobbler.swift` houses the shared shape: `ScrobbleTrack` (Sendable value type — artist / title / album / durationSeconds / startedAt), `ScrobbleEligibility` (pure free function — IFPI/last.fm rule: scrobble once `elapsed >= duration * 0.5` OR `elapsed >= 240`, whichever first, with tracks < 30s never eligible), the `ScrobblerService` protocol (`displayName` / `isReady` / `updateNowPlaying` / `scrobble`), and `ScrobblerCoordinator` (`@MainActor`, owns per-track `Pending { videoId, track, nowPlayingSent, scrobbled }` so each event fires exactly once per track — track change → reset, paused → no now-playing, threshold cross → single scrobble dispatch fan-out to every ready service, services off due to credentials/toggle are silently skipped). Also `Scrobblers.formEncode` (RFC3986 unreserved set, deterministic alphabetical key order so tests + last.fm signature share one path), `Scrobblers.makeSession()` (10s/30s timeouts mirroring InnerTubeClient.defaultSession() — scrobble POSTs land in 100-500ms; long hangs would just stack scrobble Tasks on a flaky network), and `ScrobblerKeychain` / `ScrobblerDefaults` namespaces so credentials and toggles never collide across services. `LastFmScrobbler.swift` is a `Sendable` `final class` implementing the API 2.0 spec: MD5-signed POSTs (`signature(params:secret:)` concatenates sorted-key (key+value) pairs then appends shared secret then hashes — verified in tests against an independently-computed MD5), the standard five-param scrobble shape (`method` / `api_key` / `sk` / `artist` / `track` / `timestamp`, with optional `album` and `duration`) for `track.scrobble`, the same minus timestamp for `track.updateNowPlaying`. Credentials live in Keychain under four keys (`username` / `apiKey` / `apiSecret` / `sessionKey`); BYO-API-key model means the user registers their own last.fm app at last.fm/api/account/create. Static helpers expose the desktop-app three-step OAuth flow (`fetchToken` → `authorizationURL` → `fetchSession`) so a future Settings UI can complete the dance in-app; for now the user runs the flow once externally and pastes the session key. `ListenBrainzScrobbler.swift` is the simpler sibling — single user-token (no signature, no secret), POST JSON to `/1/submit-listens` with `listen_type=single` (carries `listened_at`) or `listen_type=playing_now` (omits it). `buildPayload` returns `[String: Any]` (round-trip-tested via `JSONSerialization.data` so non-JSON values can't sneak in) with `track_metadata.additional_info.duration_ms` populated only when known. `validate(token:)` hits `/1/validate-token` so Settings can offer a Test-connection button that also captures the user's display username. Wiring: `AppEnvironment` gained `scrobbler: ScrobblerCoordinator` (lazy + `@ObservationIgnored`, same pattern as `lyricsTranslator` / `xrayCardsService`) registered with both services on first access, plus `scrobbleTrackStartedAt` (pinned on every videoId change so the eventual scrobble carries the real listen start, not the moment the threshold was crossed) and `scrobbleLastSeenVideoId`. The existing `player.onUpdate` closure (already firing the NowPlayingCenter + B9 widget snapshot updates) gained a third dispatch — builds a `ScrobbleTrack` from `currentTrack.title` / `currentTrack.subtitle` (which carries the artist string YT puts in the title row) / `Int(player.duration)` and feeds it to `scrobbler.observe(...)` with the live elapsed/duration/isPlaying tick. The closure's existing `currentTrack == nil` clear-path now also calls `scrobbler.reset()` so quitting playback closes the window cleanly. Settings → "Scrobbling" section added between Playback and Interface, with two card-style sub-sections: last.fm gets four fields (username, API key, shared secret, session key — partial-rotation saves merge against stored creds), a Clear button that zeros all four keys + flips the enabled toggle off in one shot, a clarifying paragraph about the one-off authorization dance, and a Toggle that's disabled until credentials exist. ListenBrainz gets a single user-token field, a "Test connection" button that calls `validate(token:)` and renders the result inline (captures + persists the username for display), and a matching Clear button. Both per-service toggles persist independently (`ScrobblerDefaults.lastFmEnabled` / `ScrobblerDefaults.listenBrainzEnabled`) so flipping one off doesn't disturb the other. Privacy contract honored: zero telemetry, both services off by default, credentials live in Keychain via the existing `Keychain.set`/`get`/`delete` helpers (never UserDefaults, never logs), POSTs only happen after the user has explicitly enabled a service. Strict Swift 6 preserved: both scrobblers are `Sendable` `final class` with only `let` properties (URLSession is Sendable), the coordinator is `@MainActor`, `ScrobbleTrack` and `Pending` are Sendable, Tasks spawned from MainActor capture only Sendable values. Zero new SPM dependencies (MD5 via CryptoKit's `Insecure.MD5`, JSON via `JSONSerialization`, HTTP via `URLSession`). Architectural rule untouched (no WebView surface change). Added `Tests/ScrobblerTests.swift` with 14 tests covering eligibility math (under-min-duration, 50% threshold, 240s cap, exact 30s boundary), last.fm signature (canonical input → independently-verified MD5 — recomputed via `printf … | md5sum`, lowercase-hex shape, key-ordering insensitivity), ListenBrainz payload shape (`single` carries `listened_at`, `playing_now` omits it, nil album/duration don't leak null fields, full payload round-trips through JSONSerialization), coordinator dispatch ordering (now-playing fires once per track even across many progress ticks, scrobble fires once at threshold, paused → no now-playing, new videoId resets the window, unready services are silently skipped, `reset()` clears pending), and form encoding (reserved character escapes + deterministic sort order).
- [x] **D2. On-device metadata overrides** — user edits title, artist, album per track; Riff respects override everywhere. Fixes mislabeled "feat." chaos, transliteration, classical attribution.
  - note: New `Sources/Data/Library/TrackOverrideStore.swift` is a `@MainActor @Observable final class` holding `[videoId: Override]` where `Override = { title?, artist?, album? }`. Persists as JSON under `library.trackOverrides` in `UserDefaults.standard` (JSON-encoded so the on-disk shape matches the heads-up spec verbatim; PLIST would have worked but JSON keeps the file human-inspectable for power users). Normalization rule: whitespace/empty inputs collapse to nil, and if all three fields are nil the entry is removed entirely on save — so the dict never accumulates empty stubs. `clearOverride(videoId:)` + `clearAll()` are the two delete surfaces. AppEnvironment now owns the store and exposes four helpers: `displayTitle(for:)` (returns override title when present), `displaySubtitle(for:)` (splices overridden artist/album into the first one/two " · " segments of the original subtitle, preserving year/views), `displayAlbum(for:fallback:)`, and `hasMetadataOverride(_:)`. Edit-metadata sheet (`Sources/UI/EditMetadataSheet.swift`) opens from a new "Edit metadata…" entry in `TrackContextMenu` (songs only), pre-populates the three TextFields with currently-displayed values (override winning over original), and includes a "Clear override" destructive button when one exists. Sheet mounting goes through `env.trackBeingEdited: MediaItem?` + a `.sheet(item:)` modifier on RootView so the sheet is parameterized by the row the user opened it from. Render layer updated in the four most-trafficked surfaces per the heads-up: `ThumbnailButton` (title + subtitle now flow through display helpers, pencil glyph appears when overridden), `SearchResultRow` (list rows AND `TopResultCard` hero), `NowPlayingView.queueRow` (Up Next pane), and the Now Playing big title/subtitle for the currently playing track. The pencil glyph is a 9-12pt SF Symbol in 75% white tucked next to the title; tooltip + accessibilityLabel read "Metadata edited locally" so screen readers surface the difference between the YT-canonical and user-edited rendering. Settings grew a new "Track metadata overrides" section (rendered only when non-empty, mirroring the gating of the existing blocked-artists section) listing every override with per-row Remove + a "Clear all overrides" footer. Architectural rule untouched — no WebView surface change, all override data is local. Zero new SPM dependencies. Strict Swift 6 preserved (store is `@MainActor`, `Override` is `Sendable`). Added 9 tests in `Tests/TrackOverrideStoreTests.swift` covering: full-field set + read, partial overrides keeping other fields nil, whitespace normalization to nil, all-empty-input removing the entry, empty videoId rejected, all-empty stubs dropped on load (defensive against hand-edited plists), JSON persistence round-trip across instances, clear/clear-all paths, corrupted-blob recovery, and stable alphabetical iteration via `sortedVideoIds`. Tests snapshot+restore `library.trackOverrides` in setUp/tearDown so the dev's real override JSON survives the suite.
- [x] **D3. Followed-artist feed (Bandcamp-inspired)** — chronological feed of new releases from subscribed artists.
  - note: New `InnerTubeClient.newReleasesFromFollowedArtists(limit:)` calls the existing `library(section: .artists)` plumbing to get the user's subscribed-artist list, caps at 25 (power users with hundreds of subs can stack hundreds of /browse calls otherwise), and fans out one `detail(forBrowseId:)` per artist via `withTaskGroup` so up to 25 round-trips overlap rather than serialize. Per-artist failures are caught inside each child task and logged — a single 5xx never poisons the whole rail. Each detail page's `relatedSections` are filtered by a new pure static helper `releasesFromArtistDetail(_:)` that keeps shelves whose lowercased title contains "album" / "single" / "ep" (so "Studio Albums" and "Songs and singles" both match) AND only the album-shaped items inside those shelves — song-shaped tiles that sometimes bleed into a Singles rail get filtered out because they don't carry the release-year metadata the feed sorts on. The flat union is deduped by id (an artist's collab can appear on multiple followed-artist pages) and sorted by `year` desc with nil-year items sinking to the tail and an alphabetical tie-break so SwiftUI doesn't permute rows on every refresh. LibraryView gained two `@State` fields (`followedReleases: [MediaItem]`, `followedReleasesLoading: Bool`) and renders the rail as a horizontal carousel under a "New from artists you follow" / "Recent releases, newest first" header, ABOVE the existing Mixed-for-you section, gated to `section == .artists && env.isSignedIn` so anonymous users (who have no subscriptions) and users browsing other library sections don't pay the fan-out cost. Reuses `ThumbnailButton` so tap/context-menu/hover-play match the rest of the Library grid; a 4-tile shimmer placeholder renders while the fan-out resolves. Blocked-artist filter (`env.isBlocked`) mirrored from the Mixed-for-you path so thumbed-down artists don't re-surface here through the side door. Architectural rule untouched — no WebView surface change. Zero new SPM dependencies. Strict Swift 6 preserved (`releasesFromArtistDetail` is a pure static, `withTaskGroup` captures `self` via a local `let client = self` to keep the child closures Sendable, and per-item types — MediaItem / DetailPage / HomeSection — are already Sendable). Added 3 tests in `InnerTubeParserTests.swift` (`testReleasesFromArtistDetailPicksAlbumsSinglesEPs`, `testReleasesFromArtistDetailKeywordMatchIsLenient`, `testReleasesFromArtistDetailEmptySections`) covering shelf-title matching, song-tile filtering, video / fans-might-also-like exclusion, lenient keyword matching ("Studio Albums", "Songs and singles" both match; bare "Discography" doesn't), and the empty-sections safety case.
- [x] **D4. AppleScript / Shortcuts.app expansion** — extend the App Intents foundation; add "what's playing", "play playlist X", "set rate", "open station for X".
  - note: Added 8 new `AppIntent` types to `Sources/Intents/RiffIntents.swift` alongside the Tier 2 #11 set: `WhatsPlayingIntent` (returns `ReturnsValue<String> & ProvidesDialog` — first AppIntent in Riff that hands a value back to Shortcuts, so users can pipe now-playing into "Save to a note"; reads `currentTrack.subtitle` and pulls the first `·`-separated segment as the artist, falling back to a bare title when subtitle is empty), `PlayPlaylistIntent` (library-first lookup via `env.innerTube.library(section: .playlists)` with exact → prefix → substring case-insensitive matching, then `.playlists`-filtered public-search fallback when the user is signed out OR no library playlist matches; routes through the existing `env.player.playPlaylist(id:)`), `SetPlaybackRateIntent` (clamped 0.5–2.0×, `@Parameter(... inclusiveRange: (0.5, 2.0))` so the Shortcuts UI renders a bounded numeric input, routes to existing `env.player.setPlaybackRate(_:)` which already persists per-kind), `ToggleLikeIntent` (gates on `currentTrack` and detects bridge-rollback by comparing `env.player.liked` before-and-after `toggleLike()` — when they match, the optimistic flip rolled back and we surface a clean "sign in first" error rather than reporting a silent no-op), `SkipBack15Intent` and `SkipForward30Intent` (podcast-style transport via `env.player.skip(by:)` with signed offsets; the JS bridge handles the boundary clamps at 0 and `duration`), `SetVolumeIntent` (`@Parameter(... inclusiveRange: (0, 100))` Int percent → divides to 0.0…1.0 for the bridge so the Shortcuts UI matches user mental models), and `OpenStationForIntent` (artist-first search with a `namesAreSimilar(_:_:)` Jaccard-overlap gate at ≥0.5 plus substring containment so a clean artist name auto-routes to `playArtistRadio(id:)` BUT a genre seed like "lo-fi" or "synthwave" gracefully falls back to a song search + `play(item:)`, which carries the `RDAMVM…` playlist that triggers server-generated radio; this two-step gating is the load-bearing policy that prevents "open a station for synthwave" from getting hijacked by an unrelated artist YT happened to top-rank). All 8 intents are `@MainActor`, follow the existing `liveEnvironment()` → `env.player...` → `IntentDialog(...)` pattern verbatim, and route through existing bridge entry points — no new player surface. `RiffAppShortcuts.appShortcuts` extended with 8 new `AppShortcut` registrations including phrases for each (3 phrases per intent, all using `\(.applicationName)` for app routing per the Tier 2 #11 lesson; String params do NOT interpolate the parameter into the phrase template — the system prompts conversationally instead, same gotcha noted in the original `PlayTrackIntent` comment) and per-intent `shortTitle` + `systemImageName` for the Shortcuts UI. Tests in new `Tests/RiffIntentsTests.swift` cover the `OpenStationForIntent.namesAreSimilar` helper across 6 cases: equal strings, substring containment, Jaccard ≥0.5 ("John Mayer Trio" vs "John Mayer" → 2/3 overlap), low-overlap rejection (genre seed vs unrelated artist), empty-input safety (the contract that this never crashes — `perform()` rejects whitespace earlier so the empty case is unreachable in practice), and a known-limitation regression test for punctuation stylization ("A.C.E" vs "ace" doesn't match — documented so a future contributor doesn't quietly "fix" it the wrong way). Strict Swift 6 preserved (all intents `@MainActor`; struct types are trivially `Sendable`). Zero new SPM dependencies. Architectural rule untouched — no WebView surface change, all intents route through the existing PlayerBridge surface. The `perform()` bodies that touch live network + `AppEnvironment.current` aren't unit-testable without a much heavier harness; the pure-helper similarity gate is the only inspectable policy that affects user behavior on the new intents.
- [ ] **D5. Local-web Remote (Cider-inspired)** — phone-as-remote via a local web page over Wi-Fi.
- [ ] **D6. Hum-to-Search via ShazamKit** — tap waveform icon, record 10s, ShazamKit matches, route the result through InnerTube.search. Apple-native APIs, no audio-extraction concerns.
- [ ] **D7. Auto context-mode (Endel-inspired, BYO-LLM-routed)** — time-of-day + calendar + focus state auto-selects a YT Music mood mix.
- [ ] **D8. Drag-to-reorder Up Next** — SwiftUI onMove on the upNext list.
- [ ] **D9. EQ presets** — BiquadFilterNode chain in the existing Web Audio graph we built for volume normalization; ship 5-6 presets.

### Tier E: Previously-skipped, user-reopened (scope-with-blockers)

User reopened these on 2026-06-18 after they had been initially deferred. Each one has a real blocker; the scope below is what we CAN realistically ship within Riff's constraints (no audio extraction, no telemetry server, no Premium paywall data).

- [ ] **E1. Blend / Jam / Friend Activity** — full implementation needs identity + a sync server (incompatible with no-telemetry rule). **Scoped target:** local-network co-listening over Bonjour. Two Riff instances on the same Wi-Fi can discover each other, share an Up Next queue, and one drives playback for both (the other mirrors via the local-web Remote primitive — depends on D5). A "Friend Activity" rail surfaces who is currently listening on the LAN. No cloud, no identity beyond a per-instance display name.
- [ ] **E2. Samples vertical short-video feed** — full vertical-TikTok UX is awkward on a 27" Mac display. **Scoped target:** a "Discover clips" horizontal rail on the Explore tab — same data source (30-second music-video previews from /next continuations), but laid out as a horizontal carousel with autoplay-on-hover. Tap to play the full song; star to add to a Discover playlist. Keep the swipeable-card variant behind a Settings toggle for the users who DO want full vertical.
- [ ] **E3. Background "new release" notifications** — macOS doesn't run our process in the background without a LaunchAgent (which has user-trust implications). **Scoped target:** foreground-only refresh — when Riff is running, poll subscribed-artists once an hour and post a UserNotification for any release dated in the last 24h. No background daemon. Document the trade-off (must keep Riff open to receive) in the Settings toggle copy.
- [ ] **E4. Local file playback** — large scope (file picker, indexer, mixed queue, metadata extraction, format support). **Scoped first cut:** drag-and-drop a single file onto the player to play it via a separate AVPlayer (NOT the WKWebView audio path). No library indexing yet. Mixed queue (local + YT in same Up Next) is deferred until format coverage settles. ID3 tag extraction via AVAsset for the Now Playing strip.
- [ ] **E5. Crossfade between tracks (covers part of "AutoMix")** — beat-matched AutoMix needs raw audio WKWebView hides; vocal attenuation for karaoke needs DSP on raw audio. **Scoped target:** the JS-side volume crossfade already in B8. Track this as the realistic delivery; mark beat-match + vocal attenuation as permanently blocked by the architectural rule. If a future re-architecture surfaces raw audio (e.g., InnerTube returns a direct stream URL), revisit then.
- [ ] **E6. Personal timestamped annotations on tracks** — public-track-wide comments require a server graph we don't run, and YT doesn't expose its own comment graph for arbitrary tracks via InnerTube. **Scoped target:** PRIVATE timestamped notes per track ("the drop", "use for intro"), stored locally per videoId. Renders as small markers on the scrubber. Personal-only — no sharing, no public surface.

Each E-item carries a real trade-off in its scope statement — the Ralph agent picking these up should respect those scopes and not try to ship the full-fat version that the original Skip-list entry rejected.

### Permanently blocked (no realistic scope under current architecture)

- **Beat-matched AutoMix** — needs raw audio access (time-stretch, beat detection). WKWebView's `<video>` element exposes volume control only. Re-evaluate if we ever stop using the WebView as the audio engine.
- **Vocal attenuation in karaoke mode** — same underlying constraint (DSP on raw PCM). The Lyrics tab still ships a karaoke surface (B2) without the vocal-pull-down.
- **Public comments on YT tracks** — no source. We don't run a server with a comment graph; YT's own comment data isn't exposed via InnerTube for music tracks (only for the video twin, and not via any of the endpoints we currently call). Personal annotations (E6) are the achievable alternative.

## Sequencing recommendation

**Week 1**: A1 (bugs) + A2 (UX wins). Pure cleanup.

**Week 2**: A4 (audio-to-video) + A5 (other performances) + A8 (block inline) + A9 (mood chips). Free wins from data we already fetch.

**Week 3**: A6 (Mini Player window) + A7 (Menu bar). macOS-native chrome.

**Week 4**: A3 (sleep modes) + B1 (Always-on Stats) + B6 (Pinned Library). Library polish.

Beyond that: pick from Tier B in any order. Tier C (BYO-LLM-heavy) is the differentiation play once the foundation feels solid. Tier D is opportunistic — D1 (scrobbling) is the cheapest and most-distinctive.
